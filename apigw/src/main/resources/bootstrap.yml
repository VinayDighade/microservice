spring:
  application:
    name: apigw-oc-service
Google: OK
zuul:
  ignored-headers:
    - Access-Control-Allow-Credentials
    - Access-Control-Allow-Origin
  sensitive-headers:
    - Cookie
    - Set-Cookie
  routes:
    Alice:
      path: /Alice/**
      url: http://alice-oc-service:8080/Alice
ribbon:
  eureka:
    enabled: false
server:
  port: 8092

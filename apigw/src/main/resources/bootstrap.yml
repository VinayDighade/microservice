spring:
  application:
    name: apigw-oc-service
Google: OK

zuul:
  ignored-headers:
    - Access-Control-Allow-Credentials
    - Access-Control-Allow-Origin
  sensitive-headers:
    - Cookie
    - Set-Cookie
  routes:
    Alice:
      path: /Alice/**
      url: http://localhost:8082/Alice
  force-original-query-string-encoding: true
ribbon:
  eureka:
    enabled: false
server:
  port: 8092
debug: true